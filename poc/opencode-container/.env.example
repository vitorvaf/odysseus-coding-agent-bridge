# OCAB OpenCode Runner — placeholders for secrets.
# Used by the standalone compose.yaml in this directory (ad-hoc checks).
# In production, the bridge service injects OCAB_RUNNER_TOKEN through the
# Docker secret 'ocab_runner_token' — see /compose.yaml at the repo root.

OCAB_RUNNER_TOKEN=__SET_ME__
